const createApplication = require("./createApplication");
const createArgs = require("./createArgs");
const helpArgs = require("./helpArgs");
const invalidArgs = require("./invalidArgs");
const noArgs = require("./noArgs");

module.exports = {
  createApplication,
  createArgs,
  helpArgs,
  invalidArgs,
  noArgs
};
